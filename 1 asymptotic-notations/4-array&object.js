// TODO Analyzing Performance of Arrays and Objects

// ** Objects
// ? Objects are Key value Pairs!
// ? When you don't need order
// ? When you need fast access/ insertion nd removal

// ! BIG O of objects
// ? Insertion  O(1)
// ? Removal    O(1)
// ? Searching  O(N)
// ? Access     O(1)

// ! BIG O of object methods
// ? Object.keys     O(N)
// ? Object.values   O(N)
// ? Object.entries  O(N)
// ? hasOwnProperty  O(1)

// **Arrays
// ? Ordered lists!
// ? When you need order
// ? when you need fast access/ insertion and removal(sort of...)

// ! BIG O of arrays
// ? Insertion  It depends... inFront O(n) inEnd O(1)
// ? Removal    It depends... inFront O(n) inEnd O(1)
// ? Searching  O(N)
// ? Access     O(1)

// ! Big O of Array Operations
// ? push O(1)
// ? pop O(1)
// ? shift O(N)
// ? unshift O(N)
// ? concat O(N)
// ? slice O(N)
// ? splice O(N)
// ? sort O(N*logN)
// ? forEach/map/filter/reduce/etc-  O(N)
